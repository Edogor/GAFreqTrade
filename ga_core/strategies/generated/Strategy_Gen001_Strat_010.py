# pragma pylint: disable=missing-docstring, invalid-name, pointless-string-statement
# flake8: noqa: F401
# isort: skip_file
# AUTO-GENERATED STRATEGY - DO NOT EDIT MANUALLY
# Generated: 2026-02-12T22:32:18.199044
# Generation: 1
# Strategy ID: Gen001_Strat_010

import numpy as np
import pandas as pd
from datetime import datetime, timedelta, timezone
from pandas import DataFrame
from typing import Dict, Optional, Union, Tuple
from functools import reduce

from freqtrade.strategy import (
    IStrategy,
    Trade,
    Order,
    PairLocks,
    informative,
    BooleanParameter,
    CategoricalParameter,
    DecimalParameter,
    IntParameter,
    RealParameter,
    timeframe_to_minutes,
    timeframe_to_next_date,
    timeframe_to_prev_date,
    merge_informative_pair,
    stoploss_from_absolute,
    stoploss_from_open,
    AnnotationType,
)

import talib.abstract as ta
from technical import qtpylib


class Strategy_Gen001_Strat_010(IStrategy):
    """
    Auto-generated strategy using 6 indicators
    
    AUTO-GENERATED by GAFreqTrade
    Generation: 1
    Parent Strategies: None (Random)
    """
    
    INTERFACE_VERSION = 3
    
    # Basic Settings
    timeframe = "30m"
    can_short: bool = False
    
    # ROI table
    minimal_roi = {'5': 0.13, '25': 0.079, '47': 0.044, '107': 0.029}
    
    # Stoploss
    stoploss = -0.105
    
    # Trailing stoploss
    trailing_stop = True
    trailing_stop_positive = 0.014
    trailing_stop_positive_offset = 0.018
    trailing_only_offset_is_reached = True
    
    # Run "populate_indicators()" only for new candle
    process_only_new_candles = True
    
    # Exit signal
    use_exit_signal = True
    exit_profit_only = False
    ignore_roi_if_entry_signal = False
    
    # Number of candles the strategy requires before producing valid signals
    startup_candle_count: int = 30
    
    # Strategy parameters
    buy_rsi_threshold = IntParameter(9, 49, default=29, space='buy')
    cci_sell_threshold = IntParameter(61, 101, default=81, space='buy')
    
    # Order types
    order_types = {
        "entry": "limit",
        "exit": "limit",
        "stoploss": "market",
        "stoploss_on_exchange": False
    }
    
    order_time_in_force = {
        "entry": "GTC",
        "exit": "GTC"
    }
    
    def informative_pairs(self):
        return []
    
    def populate_indicators(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """
        Adds TA indicators to the given DataFrame
        """
        # CCI
        dataframe["cci"] = ta.CCI(dataframe, timeperiod=29)

        # MACD
        macd = ta.MACD(dataframe, fastperiod=16, slowperiod=22, signalperiod=9)
        dataframe["macd"] = macd["macd"]
        dataframe["macdsignal"] = macd["macdsignal"]
        dataframe["macdhist"] = macd["macdhist"]

        # RSI
        dataframe["rsi"] = ta.RSI(dataframe, timeperiod=19)

        # ATR
        dataframe["atr"] = ta.ATR(dataframe, timeperiod=19)

        # ADX
        dataframe["adx"] = ta.ADX(dataframe, timeperiod=10)

        # Stochastic
        stoch = ta.STOCH(dataframe, fastk_period=6, slowk_period=5, slowd_period=2)
        dataframe["slowk"] = stoch["slowk"]
        dataframe["slowd"] = stoch["slowd"]

        
        return dataframe
    
    def populate_entry_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """
        Based on TA indicators, populates the entry signal for the given dataframe
        """
        conditions = []
        
        conditions.append(dataframe["rsi"] < self.buy_rsi_threshold.value)
        conditions.append((qtpylib.crossed_above(dataframe["slowk"], dataframe["slowd"])))
        conditions.append((qtpylib.crossed_above(dataframe["macd"], dataframe["macdsignal"])))
        
        if conditions:
            dataframe.loc[
                reduce(lambda x, y: x & y, conditions),
                'enter_long'
            ] = 1
        
        return dataframe
    
    def populate_exit_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """
        Based on TA indicators, populates the exit signal for the given dataframe
        """
        conditions = []
        
        conditions.append(dataframe["cci"] > self.cci_sell_threshold.value)
        
        if conditions:
            dataframe.loc[
                reduce(lambda x, y: x & y, conditions),
                'exit_long'
            ] = 1
        
        return dataframe
